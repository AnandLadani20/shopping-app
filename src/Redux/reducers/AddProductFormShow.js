const initialValue = false;

export const desertForm = (state = initialValue, action) => {
    if (action.type === "DesertProduct") {
        return true;
    } else {
        return state;
    }
};

export const miniCoolersForm = (state = initialValue, action) => {
    if (action.type === "MiniCoolersProduct") {
        return true;
    } else {
        return state;
    }
};

export const otherAirCoolersForm = (state = initialValue, action) => {
    if (action.type === "OtherAirCoolersProduct") {
        return true;
    } else {
        return state;
    }
};

export const portableCoolersForm = (state = initialValue, action) => {
    if (action.type === "PortableCoolersProduct") {
        return true;
    } else {
        return state;
    }
}

export const towerCoolersForm = (state = initialValue, action) => {
    if (action.type === "TowerCoolersProduct") {
        return true;
    } else {
        return state;
    }
}

export const windowCoolersForm = (state = initialValue, action) => {
    if (action.type === "WindowCoolersProduct") {
        return true;
    } else {
        return state;
    }
}

export const airIonizersForm = (state = initialValue, action) => {
    if (action.type === "AirIonizersProduct") {
        return true;
    } else {
        return state;
    }
}

export const carAirPurifierForm = (state = initialValue, action) => {
    if (action.type === "CarAirPurifierProduct") {
        return true;
    } else {
        return state;
    }
}
export const charcoalAirPurifierForm = (state = initialValue, action) => {
    if (action.type === "CharcoalAirPurifierProduct") {
        return true;
    } else {
        return state;
    }
}
export const electroAirPurifierForm = (state = initialValue, action) => {
    if (action.type === "ElectroAirPurifierProduct") {
        return true;
    } else {
        return state;
    }
}
export const hepaAirPurifierForm = (state = initialValue, action) => {
    if (action.type === "HepaAirPurifierProduct") {
        return true;
    } else {
        return state;
    }
}
export const otherAirPurifierForm = (state = initialValue, action) => {
    if (action.type === "OtherAirPurifierProduct") {
        return true;
    } else {
        return state;
    }
}
export const portableAirPurifierForm = (state = initialValue, action) => {
    if (action.type === "PortableAirPurifierProduct") {
        return true;
    } else {
        return state;
    }
}
export const ulpaAirPurifierForm = (state = initialValue, action) => {
    if (action.type === "UlpaAirPurifierProduct") {
        return true;
    } else {
        return state;
    }
}

export const otherDehumidifiersForm = (state = initialValue, action) => {
    if (action.type === "OtherDehumidifiersProduct") {
        return true;
    } else {
        return state;
    }
}

export const otherDeodorisersForm = (state = initialValue, action) => {
    if (action.type === "OtherDeodorisersProduct") {
        return true;
    } else {
        return state;
    }
}

export const blowerFansForm = (state = initialValue, action) => {
    if (action.type === "BlowerFansProduct") {
        return true;
    } else {
        return state;
    }
}

export const boxFansForm = (state = initialValue, action) => {
    if (action.type === "BoxFansProduct") {
        return true;
    } else {
        return state;
    }
}

export const ceilingFansForm = (state = initialValue, action) => {
    if (action.type === "CeilingFansProduct") {
        return true;
    } else {
        return state;
    }
}

export const clipFansForm = (state = initialValue, action) => {
    if (action.type === "ClipFansProduct") {
        return true;
    } else {
        return state;
    }
}

export const drumFansForm = (state = initialValue, action) => {
    if (action.type === "DrumFansProduct") {
        return true;
    } else {
        return state;
    }
}

export const exhaustFansForm = (state = initialValue, action) => {
    if (action.type === "ExhaustFansProduct") {
        return true;
    } else {
        return state;
    }
}

export const floorFansForm = (state = initialValue, action) => {
    if (action.type === "FloorFansProduct") {
        return true;
    } else {
        return state;
    }
}

export const otherFansForm = (state = initialValue, action) => {
    if (action.type === "OtherFansProduct") {
        return true;
    } else {
        return state;
    }
}

export const pedestalFansForm = (state = initialValue, action) => {
    if (action.type === "PedestalFansProduct") {
        return true;
    } else {
        return state;
    }
}

export const personalFansForm = (state = initialValue, action) => {
    if (action.type === "PersonalFansProduct") {
        return true;
    } else {
        return state;
    }
}

export const tableFansForm = (state = initialValue, action) => {
    if (action.type === "TableFansProduct") {
        return true;
    } else {
        return state;
    }
}

export const towerFansForm = (state = initialValue, action) => {
    if (action.type === "TowerFansProduct") {
        return true;
    } else {
        return state;
    }
}

export const vortexActionFansForm = (state = initialValue, action) => {
    if (action.type === "VortexActionFansProduct") {
        return true;
    } else {
        return state;
    }
}

export const wallMountedFansForm = (state = initialValue, action) => {
    if (action.type === "WallMountedFansProduct") {
        return true;
    } else {
        return state;
    }
}

export const windowFansForm = (state = initialValue, action) => {
    if (action.type === "WindowFansProduct") {
        return true;
    } else {
        return state;
    }
}

export const otherHumidifierForm = (state = initialValue, action) => {
    if (action.type === "OtherHumidifierProduct") {
        return true;
    } else {
        return state;
    }
}

export const singleRoomHumidifierForm = (state = initialValue, action) => {
    if (action.type === "SingleRoomHumidifierProduct") {
        return true;
    } else {
        return state;
    }
}

export const wholeHouseHumidifierForm = (state = initialValue, action) => {
    if (action.type === "WholeHouseHumidifierProduct") {
        return true;
    } else {
        return state;
    }
}

export const otherHeatNCoolingForm = (state = initialValue, action) => {
    if (action.type === "OtherHeatNCoolingProduct") {
        return true;
    } else {
        return state;
    }
}

export const airPuriPartNAccessForm = (state = initialValue, action) => {
    if (action.type === "AirPuriPartNAccessProduct") {
        return true;
    } else {
        return state;
    }
}

export const dehumidPartNAccessForm = (state = initialValue, action) => {
    if (action.type === "DehumidPartNAccessProduct") {
        return true;
    } else {
        return state;
    }
}

export const evaCoolerPartNAccessForm = (state = initialValue, action) => {
    if (action.type === "EvapCoolerPartNAccessProduct") {
        return true;
    } else {
        return state;
    }
}

export const fanPartNAccessForm = (state = initialValue, action) => {
    if (action.type === "FanPartNAccessProduct") {
        return true;
    } else {
        return state;
    }
}

export const heaterPartNAccessForm = (state = initialValue, action) => {
    if (action.type === "HeaterPartNAccessProduct") {
        return true;
    } else {
        return state;
    }
}

export const humidiPartNAccessForm = (state = initialValue, action) => {
    if (action.type === "HumidiPartNAccessProduct") {
        return true;
    } else {
        return state;
    }
}

export const replaceBelthumidiForm = (state = initialValue, action) => {
    if (action.type === "ReplaceBelthumidiProduct") {
        return true;
    } else {
        return state;
    }
}

export const otherPartNAccessForm = (state = initialValue, action) => {
    if (action.type === "OtherPartNAccessProduct") {
        return true;
    } else {
        return state;
    }
}

export const electricHeatersForm = (state = initialValue, action) => {
    if (action.type === "ElectricHeatersProduct") {
        return true;
    } else {
        return state;
    }
}

export const fanHeatersForm = (state = initialValue, action) => {
    if (action.type === "FanHeatersProduct") {
        return true;
    } else {
        return state;
    }
}

export const halogenHeatersForm = (state = initialValue, action) => {
    if (action.type === "HalogenHeatersProduct") {
        return true;
    } else {
        return state;
    }
}

export const heatConvertorsHeatersForm = (state = initialValue, action) => {
    if (action.type === "HeatConvertorsProduct") {
        return true;
    } else {
        return state;
    }
}

export const keroseneSpaceHeatersHeatersForm = (state = initialValue, action) => {
    if (action.type === "KeroseneSpaceHeatersProduct") {
        return true;
    } else {
        return state;
    }
}

export const naturalgasSpaceHeatersForm = (state = initialValue, action) => {
    if (action.type === "NaturalgasSpaceHeatersProduct") {
        return true;
    } else {
        return state;
    }
}

export const otherRoomHeatersForm = (state = initialValue, action) => {
    if (action.type === "OtherRoomHeatersProduct") {
        return true;
    } else {
        return state;
    }
}

export const propaneSpaceHeatersForm = (state = initialValue, action) => {
    if (action.type === "PropaneSpaceHeatersProduct") {
        return true;
    } else {
        return state;
    }
}

export const immersionRodsForm = (state = initialValue, action) => {
    if (action.type === "ImmersionRodsProduct") {
        return true;
    } else {
        return state;
    }
}

export const storageWaterHeatersForm = (state = initialValue, action) => {
    if (action.type === "StorageWaterHeatersProduct") {
        return true;
    } else {
        return state;
    }
}

export const instantWaterHeatersForm = (state = initialValue, action) => {
    if (action.type === "InstantWaterHeatersProduct") {
        return true;
    } else {
        return state;
    }
}